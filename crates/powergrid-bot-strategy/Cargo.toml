[package]
name = "powergrid-bot-strategy"
version = "0.1.0"
edition = "2021"

[dependencies]
powergrid-core = { path = "../powergrid-core" }
serde = { workspace = true }
tracing = { workspace = true }
rand = { version = "0.8", features = ["small_rng"] }
